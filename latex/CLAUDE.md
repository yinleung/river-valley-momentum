# Paper-Writing Mode (latex/CLAUDE.md)

You are working on paper text. Apply the standards below to all body prose, figure captions, remarks, tables, etc. The first import is the general standard; the second is this paper's project-specific overrides (term table, banned phrases, dead labels).

@./WRITING.md
@./CRITERION.md

Reminders: every numerical claim is checked against its source artifact before it ships, and the project `CRITERION.md` overrides the general `WRITING.md` wherever the two differ.

Source formatting: in `.tex` files, one sentence per line — never hard-wrap within a sentence; display math keeps its own multi-line layout.
