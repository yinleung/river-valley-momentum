# Codebase Index

One row per integrated task. Add a row when an agent finishes wiring a new codebase (see `CODING.md` Pillar 1). Keep it current — this is the single place to discover what lives here.

| Task | Upstream source | Status | Integrated | Notes |
|------|-----------------|--------|-----------|-------|
| toy-river-valley | first-party (no upstream) | production | 2026-06-25 (ext. 2026-07-04) | E1–E5 controlled demos plus extensions: `scripts/run_e[1-5]_*.py`, `run_e3_heatmap.py` (CL-2 grid + β window), `run_e7_optimizer.py` part 1 (closed-loop β sweeps + schedule/adaptive arms), `run_e8_headline.py` (regime figure), `run_e9_betaopt.py` (E9 β-optimization sweep + normalization ablation), `run_e10_bendwindow.py` (E10 β window vs bend frequency), `run_e13_forced.py` (E13 forced-disturbance controls on the FR gain guides; task `forced_controls`, 4 gates PASS 28/28 cells); guides from `core/closedloop.py` (now incl. `forced_gain`/`resonant_frequency`/`forced_hill_loss`). |
| mlp-diagnostic | first-party (no upstream) | production | 2026-06-25 (ext. 2026-07-04) | E6 real-network probe + onset re-analysis, and E7 closed-loop optimizer tests via the Contract's `end_to_end_train` (SGDM + Muon pre/post/polar in both stream regimes). Gradients show ω=π hill oscillation (HFER 0.99, onset at step ~200); momentum lifts slow-gradient alignment 0.26→0.82. |
| nanogpt | github.com/karpathy/nanoGPT @ 3adf61e | production | 2026-07-04 | E11 scale transfer: 0.40M-param char-GPT on shakespeare_char, mini-batch EMA-SGDM sweep + Muon pre/post/polar via `adapter/contract.py` and `scripts/run_e11_scale.py` (MPS, ~2 h). Gates A/C/E PASS (HFER 0.82–0.84 at every stable lr; benefit 0.8→21.8% with lr; pre-polar wins), B/D FAIL as operationalized (β=0 divergence seed-marginal at lr=0.4; mechanism-score ranking does not transfer); val tracks train (ρ=0.90). E12 decomposition (`adapter/decomp.py` + `scripts/run_e12_decomp.py`, ~20 min): gates A/B/C PASS — high-band state share 0.83–0.93, residual white (HFER 0.400–0.409 vs 0.402), top-16 curvature dirs carry 0.82–0.92 of the state high band; restricted ηλ_top 0.21–0.42. |
