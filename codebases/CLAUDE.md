# Code Mode (Claude)

You are working on code, experiments, or figures. Apply the standard below to everything under `codebases/`.

@./CODING.md

Reminders: keep dropped-in repos pristine under `<task>/upstream/` and integrate only via `<task>/adapter/`; every run logs under the unified naming scheme with its git SHA; figures read `results/cache/`, never the live tracker; version with git, never `_v2` filenames or parallel
output directories.
